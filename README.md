# Steps
```
chmod +x screenshot.sh
chmod -x screenshot-window.sh
chmod +x screenshot-region.sh

# copy
cp screenshot.sh ~/scripts
cp screenshot-window.sh ~/scripts
cp screenshot-region.sh ~/scripts

# at the top of i3wm config file
# vim:ft=i3config:

# at the top of a shell script
#!/bin/bash
```